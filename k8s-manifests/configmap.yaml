apiVersion: v1
kind: ConfigMap
metadata:
  name: gold-profit-app-config
  namespace: gold-app
data:
  APP_NAME: "gold-profit-app"
  APP_ENV: "demo"
  PORT: "8080"
  DATA_DIR: "/app/data"
  DB_PATH: "/app/data/gold.db"
