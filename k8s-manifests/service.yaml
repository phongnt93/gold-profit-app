apiVersion: v1
kind: Service
metadata:
  name: gold-profit-app
  namespace: gold-app
  labels:
    app: gold-profit-app
spec:
  selector:
    app: gold-profit-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  # muốn NodePort debug thì thêm:
  # type: NodePort
  # nodePort: 30081
